const menu=document.querySelector('.menu-btn'); const panel=document.querySelector('.mobile-panel'); if(menu){menu.addEventListener('click',()=>panel.classList.toggle('open'))}
function animateHeading(){document.querySelectorAll('[data-animate-heading]').forEach(h=>{const text=h.dataset.text||h.textContent; h.textContent=''; const lines=text.split('\\n'); const charDelay=30; let total=0; lines.forEach((line,li)=>{const lineEl=document.createElement('span'); lineEl.style.display='block'; [...line].forEach((ch,ci)=>{const span=document.createElement('span'); span.className='char'; span.textContent=ch===' '?'\u00A0':ch; span.style.transitionDelay=`${200+(total+ci)*charDelay}ms`; lineEl.appendChild(span)}); total+=line.length; h.appendChild(lineEl)}); setTimeout(()=>h.querySelectorAll('.char').forEach(c=>c.classList.add('show')),80)})}
function fadeIn(){document.querySelectorAll('.fade').forEach(el=>{const d=+el.dataset.delay||0; setTimeout(()=>el.classList.add('show'),d)})}
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in'); io.unobserve(e.target)}}),{threshold:.14}); document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
document.querySelectorAll('form[data-booking]').forEach(form=>{form.addEventListener('submit',e=>{e.preventDefault(); const data=Object.fromEntries(new FormData(form).entries()); const subject=encodeURIComponent('Aisosa Technology Website Enquiry / Booking'); const body=encodeURIComponent(`Name: ${data.name||''}\nPhone: ${data.phone||''}\nEmail: ${data.email||''}\nService: ${data.service||''}\nPreferred Date: ${data.date||''}\nMessage: ${data.message||''}`); document.querySelector('.modal').classList.add('show'); form.reset(); setTimeout(()=>{window.location.href=`mailto:hello@aisosatechnology.com?subject=${subject}&body=${body}`},700)})});
document.querySelectorAll('.close-modal').forEach(b=>b.addEventListener('click',()=>document.querySelector('.modal').classList.remove('show')));
animateHeading(); fadeIn();
